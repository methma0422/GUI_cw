<Window x:Class="RuchiToursRemake.EditGuest"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RuchiToursRemake"
        mc:Ignorable="d"
        Title="EditGuest" Height="800" Width="950">
    <Grid Style="{StaticResource uCtrlGrid}">
        <Rectangle Style="{StaticResource EditPageRectangle}">
            <Rectangle.Effect>
                <BlurEffect/>
            </Rectangle.Effect>
        </Rectangle>

        <Label Style="{StaticResource MainTitles}" Content="Edit Guest Details" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,117,0,0"/>
        <Label Style="{StaticResource userCtrlLbls}" Content="Full Name" Margin="230,262,0,0"/>
        <Label Style="{StaticResource userCtrlLbls}" Content="Contact No" Margin="230,326,0,0"/>
        <Label Style="{StaticResource userCtrlLbls}" Content="E-mail" Margin="230,390,0,0"/>
        <Label Style="{StaticResource userCtrlLbls}" Content="Country" Margin="230,454,0,0"/>
        <Label Style="{StaticResource userCtrlLbls}" Content="Passport No" Margin="230,518,0,0"/>
        <TextBox x:Name="TxtFullName" Style="{StaticResource userCtrlTxtBox}" Margin="396,262,0,0" Width="280"/>
        <TextBox x:Name="TxtContactNo" Style="{StaticResource userCtrlTxtBox}" Margin="396,326,0,0" Width="280"/>
        <TextBox x:Name="TxtEmail" Style="{StaticResource userCtrlTxtBox}" Margin="396,390,0,0" Width="280"/>
        <TextBox x:Name="TxtCountry" Style="{StaticResource userCtrlTxtBox}" Margin="396,454,0,0" Width="280"/>
        <TextBox x:Name="TxtPassportNo" Style="{StaticResource userCtrlTxtBox}" Margin="396,518,0,0" Width="280"/>
        <Button x:Name="BtnSave" Style="{StaticResource userCtrlBtn}" Content="Save" Width="76" Margin="600,616,0,0" Click="BtnSave_Click"/>
    </Grid>
</Window>
